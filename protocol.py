Input:
.yaml file (contains parameters for input into the model. And which model to use)

Output:
dictionary (parses the parameters for input into the model)
which model chosen

Protocols script for converting a file input into instructions for the Model code.